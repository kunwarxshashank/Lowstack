version: "3.9"

services:
  nextjs:
    build: .
    container_name: lowstack
    ports:
      - "3016:3016"
    restart: unless-stopped
    env_file:
      - .env

    develop:
      watch:
        - action: rebuild
          path: .
